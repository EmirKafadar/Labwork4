public class Administrator extends User {
    public Administrator(String id, String name, String surname, String username, String password, String email, Address address) {
        super(id, name, surname, username, password, email, address);
    }
}
